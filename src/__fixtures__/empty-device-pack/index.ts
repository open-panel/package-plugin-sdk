// Declares contributes.devices and exports no driver — the silent no-device
// install the registry has to turn into a named problem.
import { definePlugin } from "../../define-plugin.js";

export default definePlugin({});
